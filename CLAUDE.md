@AGENTS.md

<!--
  Claude bridge. Claude Code reads CLAUDE.md, not AGENTS.md, so this file imports the
  canonical AGENTS.md (@AGENTS.md is the portable default — works on Windows too, where
  symlinks would need Administrator / Developer Mode). Keep AGENTS.md as the single
  source of agent instructions; add Claude-only directives in a `## Claude Code` section
  below if this repo ever needs any.
-->
